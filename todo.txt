FIX Name generation           <- Seems there are erratically associated numbers associated to characters. Where are we incrementing `characterCount`?
FIX Counter                   <- Counter needs to kill.
FIX Intercept                 <- Interception needs to switch `Attack()` defenders. 
FIX Interception Focus        <- Interception is only possible if `this.focus > 0`. 
